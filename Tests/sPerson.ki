class Person {
  private var age : int;
  private var sex : char;
  private var name : String;
  public var height : double;

  constructor(a : int, s : char, n : String) { // Right now LOAD_PARAM quadruple does not work because objects have no addresses as of yet.
    age = a;
    sex = s;
    // name = n; This doesn't work right now because assignment of addresses to objects has not been implemented.
  }

  public_func independent int getAge() {
    return age;
  }

  public_func char getSex() {
    return sex;
  }

  public_func String getName() {
    return name;
  }

  public_func void setAge(a : int) {
    age = a;
  }

  public_func void setSex(s : char) {
    sex = s;
  }

  public_func void setName(n : String) {
    // name = n; This doesn't work right now because assignment of addresses to objects has not been implemented.
  }

  public_func void main() {
    
  }
}

Main {
  public_func independent void main() {
    print("HOLA");
  }
}