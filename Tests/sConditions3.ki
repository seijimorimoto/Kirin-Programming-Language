Main {
  public_func void main() {
    var x, y, z, w, v: int;

    if (x < y) {
      if (x < z) {
        print(x);
      } elseif (x < w) {
        print(w);
      }
    } elseif (x < w) {
      print(w);
    } else {
      print(y);
    }
    print(v);
  }
}