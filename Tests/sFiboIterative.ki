Main {
  public_func independent void main() {
    var n, i : int;
    vec arr : int[100];
    arr[0] = 1;
    arr[1] = 1;

    scan(n);

    if (n > 2) {
      for (i = 2; i < n; i = i + 1) {
        arr[i] = arr[i - 1] + arr[i - 2];
      }
    }

    print(arr[n - 1]);
  } 
}